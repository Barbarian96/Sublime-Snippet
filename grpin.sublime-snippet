<snippet>
  <content><![CDATA[
    
    //vector<int>adj[max];
    
    int node,edge;
    cin>>node>>edge;

    for(int i=0;i<edge;i++)
    {
        int n1,n2;
        cin>>n1>>n2;
        adj[n1].push_back(n2);
        adj[n2].push_back(n1);
    }
    
]]></content>
  <tabTrigger>grpin</tabTrigger>
  <description></description>
  <!-- Optional: Set a scope to limit where the snippet will trigger -->
  <!-- <scope >source.python</scope > -->
</snippet>
